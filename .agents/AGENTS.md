# Rules for Awesome AI Library

- Write all project files, portal code, UI copy, and documentation in English.
- For generic library names (like `skills` or `agent-skills`), append the author's name in parentheses (e.g., `skills (by mattpocock)` or `agent-skills (by addyosmani)`).
